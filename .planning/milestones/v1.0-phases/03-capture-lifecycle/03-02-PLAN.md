---
phase: 03-capture-lifecycle
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/renderer/app.ts
  - claude-integration/SKILL.md
autonomous: false
requirements: [INTG-05]

must_haves:
  truths:
    - "Overlay canvas draws screen image at correct scale on HiDPI displays (no zoom artifact)"
    - "SKILL.md contains promotion instructions so Claude can save important screenshots to ./screenshots/"
    - "Promotion uses mkdir -p ./screenshots && cp pattern"
    - "Claude uses descriptive filenames for promoted screenshots"
  artifacts:
    - path: "src/renderer/app.ts"
      provides: "HiDPI-correct canvas rendering"
      contains: "ctx.drawImage(screenImage, x, y, width, height, x, y, width, height)"
    - path: "claude-integration/SKILL.md"
      provides: "Screenshot promotion instructions"
      contains: "Screenshot Promotion"
  key_links:
    - from: "src/renderer/app.ts drawSelection()"
      to: "canvas drawImage source coords"
      via: "CSS pixel source coordinates (no dpr multiplication)"
      pattern: "ctx\\.drawImage\\(screenImage, x, y, width, height"
    - from: "claude-integration/SKILL.md"
      to: "./screenshots/ directory"
      via: "mkdir -p && cp bash command"
      pattern: "mkdir -p \\./screenshots"
---

<objective>
Fix HiDPI overlay rendering and add screenshot promotion instructions to SKILL.md (INTG-05).

Purpose: Fix the deferred HiDPI zoom artifact from Phase 1 so the overlay visually matches the actual screen on Retina/HiDPI displays. Add SKILL.md instructions so Claude can promote important screenshots to the project directory for long-term reference.
Output: Fixed app.ts with correct canvas source coordinates, updated SKILL.md with promotion section.
</objective>

<execution_context>
@C:/Users/jdvis/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jdvis/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-capture-lifecycle/03-CONTEXT.md
@.planning/phases/03-capture-lifecycle/03-RESEARCH.md

<interfaces>
<!-- Key code patterns the executor needs for the HiDPI fix. -->

From src/renderer/app.ts — drawSelection() line 104 (BUGGY):
```typescript
// Source coords must be in physical pixels (screenImage is at native resolution).
ctx.drawImage(screenImage, x * dpr, y * dpr, width * dpr, height * dpr, x, y, width, height);
```

From src/renderer/app.ts — transitionToPreviewing() line 134 (BUGGY):
```typescript
cropCtx.drawImage(screenImage!, x * dpr, y * dpr, width * dpr, height * dpr, 0, 0, width * dpr, height * dpr);
```

From src/renderer/app.ts — drawDimOverlay() line 74 (CORRECT — no change needed):
```typescript
ctx.drawImage(screenImage, 0, 0, w, h);
```

Root cause: thumbnailSize in getScreenSources() is set to `activeDisplay.size` which returns
CSS pixel dimensions. So screenImage natural resolution is CSS pixels, NOT physical pixels.
The `* dpr` multiplication overshoots the source image on HiDPI displays.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix HiDPI overlay canvas rendering</name>
  <files>src/renderer/app.ts</files>
  <read_first>
    - src/renderer/app.ts
    - .planning/phases/03-capture-lifecycle/03-RESEARCH.md
  </read_first>
  <action>
Fix two locations in `src/renderer/app.ts` where drawImage source coordinates incorrectly multiply by `dpr`. The screenImage is at CSS pixel resolution (thumbnailSize was set to display.size CSS values), so source coords must NOT be multiplied by dpr.

1. **drawSelection() — line 104:**
   Change:
   ```typescript
   ctx.drawImage(screenImage, x * dpr, y * dpr, width * dpr, height * dpr, x, y, width, height);
   ```
   To:
   ```typescript
   ctx.drawImage(screenImage, x, y, width, height, x, y, width, height);
   ```
   Also update the comment on line 102 from:
   ```
   // Source coords must be in physical pixels (screenImage is at native resolution).
   ```
   To:
   ```
   // Source coords in CSS pixels (screenImage resolution matches display.size CSS pixels).
   ```

2. **transitionToPreviewing() — line 134:**
   Change:
   ```typescript
   cropCtx.drawImage(screenImage!, x * dpr, y * dpr, width * dpr, height * dpr, 0, 0, width * dpr, height * dpr);
   ```
   To:
   ```typescript
   cropCtx.drawImage(screenImage!, x, y, width, height, 0, 0, width * dpr, height * dpr);
   ```
   Also update the comment on line 129 from:
   ```
   // Source coords in physical pixels, output at physical resolution for quality
   ```
   To:
   ```
   // Source coords in CSS pixels, output at physical resolution for quality preview
   ```

NOTE: drawDimOverlay() at line 74 is already correct — `ctx.drawImage(screenImage, 0, 0, w, h)` uses CSS pixel dimensions. No change needed.

NOTE: The destination coordinates in transitionToPreviewing() remain `0, 0, width * dpr, height * dpr` because the crop canvas backing buffer is sized at physical pixels (`cropCanvas.width = width * dpr`) for quality output.
  </action>
  <verify>
    <automated>cd C:/Users/jdvis/Documents/Projects/snapview && bunx tsc --noEmit -p tsconfig.renderer.json</automated>
  </verify>
  <acceptance_criteria>
    - app.ts line in drawSelection contains `ctx.drawImage(screenImage, x, y, width, height, x, y, width, height)` (no `* dpr` in source coords)
    - app.ts line in transitionToPreviewing contains `cropCtx.drawImage(screenImage!, x, y, width, height, 0, 0, width * dpr, height * dpr)` (source at CSS pixels, dest at physical pixels)
    - app.ts does NOT contain `x * dpr, y * dpr, width * dpr, height * dpr, x, y` anywhere (the old buggy pattern with dpr in source AND CSS in dest)
    - app.ts comment says "CSS pixels" not "physical pixels" for source coords in drawSelection
    - TypeScript compilation passes with no errors
  </acceptance_criteria>
  <done>Overlay canvas renders screen image at correct scale on HiDPI displays. Selection cutout and preview crop both use CSS pixel source coordinates matching the screenImage resolution.</done>
</task>

<task type="auto">
  <name>Task 2: Add screenshot promotion instructions to SKILL.md (INTG-05)</name>
  <files>claude-integration/SKILL.md</files>
  <read_first>
    - claude-integration/SKILL.md
    - .planning/phases/03-capture-lifecycle/03-CONTEXT.md
    - .planning/phases/03-capture-lifecycle/03-RESEARCH.md
  </read_first>
  <action>
Append a new section to the end of `claude-integration/SKILL.md` after the existing step 6. Add the following content:

```markdown

## Screenshot Promotion

After a screenshot is sent to context, evaluate whether it deserves long-term storage in the project.

**Offer promotion for:** design references, bug evidence, architecture diagrams, UI mockups, error screenshots the user will need to share or revisit.

**Stay quiet for:** quick terminal checks, temporary debug output, throwaway troubleshooting captures.

The user can always override with "save that screenshot" or "keep that" — always honor the request regardless of your assessment.

**To promote a screenshot:**
1. Generate a descriptive filename based on the screenshot content (e.g., `login-page-layout.png`, `api-error-response.png`, `cart-checkout-bug.png`)
2. Run via Bash: `mkdir -p ./screenshots && cp {temp_file_path} ./screenshots/{descriptive-name}.png`
3. Confirm to the user: "Saved as `./screenshots/{descriptive-name}.png`"
```

Do NOT modify the existing frontmatter or steps 1-6. Only append the new section.
  </action>
  <verify>
    <automated>cd C:/Users/jdvis/Documents/Projects/snapview && grep -c "Screenshot Promotion" claude-integration/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - SKILL.md contains `## Screenshot Promotion` heading
    - SKILL.md contains `mkdir -p ./screenshots && cp`
    - SKILL.md contains `Offer promotion for:` guidance text
    - SKILL.md contains `Stay quiet for:` guidance text
    - SKILL.md contains `descriptive-name` pattern reference
    - SKILL.md original steps 1-6 are unchanged (still contains "Launching capture UI...")
    - SKILL.md frontmatter is unchanged (still contains `disable-model-invocation: true`)
  </acceptance_criteria>
  <done>SKILL.md contains promotion instructions enabling Claude to save important screenshots to ./screenshots/ with descriptive names. Existing skill behavior is preserved.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of all Phase 3 changes</name>
  <files>src/renderer/app.ts, claude-integration/SKILL.md</files>
  <action>
Build the project and present verification steps to the user. Run `bun run build` to ensure the project compiles. Then prompt user to verify the visual and functional changes.
  </action>
  <verify>
    <automated>cd C:/Users/jdvis/Documents/Projects/snapview && bun run build && bun test</automated>
  </verify>
  <what-built>
    Phase 3 capture lifecycle: env-var cleanup (Plan 01), clipboard copy (Plan 01), HiDPI overlay fix (this plan), SKILL.md promotion (this plan).
  </what-built>
  <how-to-verify>
    1. Build: `cd C:/Users/jdvis/Documents/Projects/snapview && bun run build`
    2. Run: `snapview` — verify the overlay appears without zoom artifacts (HiDPI fix)
    3. Drag to select a region, click "Send to Claude" — verify capture completes
    4. Open any app (Slack, Paint, VS Code) and press Ctrl+V — verify the screenshot pastes correctly (clipboard copy)
    5. Run: `bun test` — verify all tests pass
    6. Optional: set `SNAPVIEW_RETENTION_HOURS=0` and run `snapview` to verify cleanup deletes all old files immediately
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues</resume-signal>
  <done>User confirms overlay renders correctly on their display, clipboard paste works, and all tests pass.</done>
</task>

</tasks>

<verification>
```bash
cd C:/Users/jdvis/Documents/Projects/snapview && bun run build && bun test
```
Build succeeds and all tests pass.
</verification>

<success_criteria>
- HiDPI overlay renders without zoom artifact (source coords use CSS pixels)
- SKILL.md contains screenshot promotion instructions
- Build passes, tests pass
- User verifies clipboard paste and overlay rendering
</success_criteria>

<output>
After completion, create `.planning/phases/03-capture-lifecycle/03-02-SUMMARY.md`
</output>
