---
phase: 02-claude-code-integration
plan: 03
type: execute
wave: 3
depends_on: [02-02]
files_modified: []
autonomous: false
requirements: [INTG-01, INTG-02, INTG-03, INTG-04, INST-01]

must_haves:
  truths:
    - "Full test suite passes (bun test)"
    - "Build succeeds (bun run build)"
    - "snapview install creates files in ~/.claude/ correctly"
    - "snapview uninstall removes files from ~/.claude/ correctly"
  artifacts: []
  key_links: []
---

<objective>
Run the full build and test suite, then perform a live integration checkpoint where the user verifies the install/uninstall flow and (optionally) tests /snapview inside a Claude Code session.

Purpose: Confirms the entire Phase 2 integration works end-to-end before marking the phase complete.
Output: Verified integration — all automated tests green, manual install/uninstall validated.
</objective>

<execution_context>
@C:/Users/jdvis/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jdvis/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-claude-code-integration/02-01-SUMMARY.md
@.planning/phases/02-claude-code-integration/02-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full build and test suite</name>
  <files></files>
  <read_first>
    - package.json (verify build and test commands)
  </read_first>
  <action>
Run the following commands in sequence and verify all pass:

1. `bun run build` — electron-vite build must succeed (produces out/ directory)
2. `bun run typecheck` — TypeScript compilation must pass with no errors
3. `bun test` — full test suite must pass (Phase 1 tests + new Phase 2 postinstall/hook tests)

If any command fails, diagnose and fix the issue before proceeding. Common issues:
- TypeScript errors from new files: check tsconfig includes
- Test failures: check temp directory cleanup, PATH mocking
- Build errors: ensure no new source files break electron-vite config

Report the exact output of each command.
  </action>
  <verify>
    <automated>bun run build && bun run typecheck && bun test</automated>
  </verify>
  <acceptance_criteria>
    - `bun run build` exits 0
    - `bun run typecheck` exits 0 with no errors
    - `bun test` exits 0 with all tests passing
  </acceptance_criteria>
  <done>Build succeeds, typecheck is clean, all tests pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Manual integration verification</name>
  <what-built>
    Complete Claude Code integration: SKILL.md template, Stop hook auto-trigger script, postinstall registration with read-modify-write settings merge, CLI install/uninstall subcommands, and package.json postinstall entry.
  </what-built>
  <how-to-verify>
    **Install verification:**
    1. Run `node bin/snapview.cjs install` from the project root
    2. Check that `~/.claude/skills/snapview/SKILL.md` exists and contains `disable-model-invocation: true`
    3. Check that `~/.claude/hooks/snapview-autotrigger.js` exists
    4. Check that `~/.claude/settings.json` has a `hooks.Stop` entry containing `snapview-autotrigger`
    5. Check that `~/.claude/settings.json` has `env.SNAPVIEW_AUTO_TRIGGER` set to `"1"`
    6. Verify any pre-existing settings.json entries were preserved (not overwritten)

    **Uninstall verification:**
    7. Run `node bin/snapview.cjs uninstall` from the project root
    8. Check that `~/.claude/skills/snapview/` directory is gone
    9. Check that `~/.claude/hooks/snapview-autotrigger.js` is gone
    10. Check that `~/.claude/settings.json` no longer has the snapview hook entry
    11. Verify any pre-existing settings.json entries are still present

    **Optional — Claude Code live test (if Claude Code is available):**
    12. Run `node bin/snapview.cjs install` again to re-register
    13. Start a new Claude Code session (restart if already running — hooks need session restart)
    14. Type `/snapview` — verify the capture UI launches
    15. Capture a region — verify Claude reads the screenshot file
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
- Full automated suite green: `bun run build && bun run typecheck && bun test`
- Human verified: install creates correct files, uninstall removes them, settings.json merge is non-destructive
</verification>

<success_criteria>
All automated checks pass. User confirms install/uninstall flow works correctly on their machine with real ~/.claude/ directory.
</success_criteria>

<output>
After completion, create `.planning/phases/02-claude-code-integration/02-03-SUMMARY.md`
</output>
