---
phase: 01-capture-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - electron.vite.config.ts
  - tsconfig.json
  - tsconfig.main.json
  - tsconfig.renderer.json
  - src/shared/types.ts
  - src/renderer/index.html
  - bin/snapview.cjs
  - .gitignore
autonomous: true
requirements:
  - INST-02
  - INST-03

must_haves:
  truths:
    - "Project builds with electron-vite without errors"
    - "CLI entry point can locate and spawn the Electron binary"
    - "Shared types are importable from both main and renderer tsconfig contexts"
  artifacts:
    - path: "package.json"
      provides: "Project manifest with electron 35.7.5, electron-vite, bin entry"
      contains: "snapview"
    - path: "electron.vite.config.ts"
      provides: "electron-vite build configuration for main/preload/renderer"
      contains: "externalizeDepsPlugin"
    - path: "src/shared/types.ts"
      provides: "RegionRect, CaptureResult, AppPhase types"
      exports: ["RegionRect", "CaptureResult", "AppPhase"]
    - path: "bin/snapview.cjs"
      provides: "CLI entry point that spawns Electron"
      contains: "spawn"
  key_links:
    - from: "bin/snapview.cjs"
      to: "out/main/index.js"
      via: "child_process.spawn with electron binary"
      pattern: "spawn.*electron.*appPath"
    - from: "package.json"
      to: "bin/snapview.cjs"
      via: "bin field"
      pattern: '"bin".*snapview'
---

<objective>
Scaffold the complete Snapview Electron project from scratch — package.json, electron-vite config, TypeScript configs, shared type definitions, renderer HTML shell, CLI entry point, and .gitignore. This establishes the project foundation that all subsequent plans build on.

Purpose: Every other plan depends on the project structure, build tooling, and type contracts being in place first.
Output: A buildable (though non-functional) Electron project skeleton with all configs, types, and the CLI launcher.
</objective>

<execution_context>
@C:/Users/jdvis/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jdvis/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-capture-engine/01-CONTEXT.md
@.planning/phases/01-capture-engine/01-RESEARCH.md
@.planning/phases/01-capture-engine/01-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create project scaffold — package.json, configs, shared types, HTML shell</name>
  <files>package.json, electron.vite.config.ts, tsconfig.json, tsconfig.main.json, tsconfig.renderer.json, src/shared/types.ts, src/renderer/index.html, .gitignore</files>
  <read_first>
    .planning/phases/01-capture-engine/01-RESEARCH.md
    .planning/phases/01-capture-engine/01-UI-SPEC.md
  </read_first>
  <action>
Create all project files from scratch (greenfield — no existing code).

**package.json:**
```json
{
  "name": "snapview",
  "version": "0.1.0",
  "description": "Screen capture tool for Claude Code",
  "license": "MIT",
  "main": "out/main/index.js",
  "bin": {
    "snapview": "bin/snapview.cjs"
  },
  "scripts": {
    "dev": "electron-vite dev",
    "build": "electron-vite build",
    "typecheck": "tsc --noEmit -p tsconfig.main.json && tsc --noEmit -p tsconfig.renderer.json",
    "test": "bun test"
  },
  "dependencies": {
    "electron": "35.7.5",
    "commander": "14.0.3"
  },
  "devDependencies": {
    "electron-vite": "5.0.0",
    "electron-builder": "26.8.1",
    "typescript": "5.9.3",
    "@types/node": "^22.0.0"
  }
}
```

**electron.vite.config.ts:**
```typescript
import { defineConfig, externalizeDepsPlugin } from 'electron-vite';

export default defineConfig({
  main: {
    plugins: [externalizeDepsPlugin()],
  },
  preload: {
    plugins: [externalizeDepsPlugin()],
  },
  renderer: {},
});
```

**tsconfig.json** (base):
```json
{
  "compilerOptions": {
    "target": "ES2022",
    "module": "ESNext",
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true,
    "resolveJsonModule": true,
    "isolatedModules": true,
    "declaration": true,
    "sourceMap": true
  }
}
```

**tsconfig.main.json:**
```json
{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "module": "ESNext",
    "moduleResolution": "nodenext",
    "outDir": "out/main",
    "rootDir": "src/main",
    "types": ["node"]
  },
  "include": ["src/main/**/*", "src/shared/**/*"]
}
```

**tsconfig.renderer.json:**
```json
{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "module": "ESNext",
    "moduleResolution": "bundler",
    "outDir": "out/renderer",
    "rootDir": "src/renderer",
    "lib": ["ES2022", "DOM", "DOM.Iterable"]
  },
  "include": ["src/renderer/**/*", "src/shared/**/*"]
}
```

**src/shared/types.ts:**
```typescript
export interface RegionRect {
  x: number;
  y: number;
  width: number;
  height: number;
}

export interface CaptureResult {
  filePath: string;
}

export type AppPhase = 'selecting' | 'previewing' | 'approved' | 'cancelled';

// IPC channel names — single source of truth for main, preload, and renderer
export const IPC_CHANNELS = {
  GET_SOURCES: 'capture:get-sources',
  CAPTURE_REGION: 'capture:region',
  CANCEL: 'capture:cancel',
} as const;
```

**src/renderer/index.html:**
```html
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Snapview</title>
  <link rel="stylesheet" href="./styles.css" />
</head>
<body>
  <canvas id="overlay-canvas"></canvas>
  <div id="hint-bar">Drag to select  &middot;  ESC to cancel</div>
  <div id="preview-panel" style="display: none;">
    <h2>Capture Preview</h2>
    <img id="preview-image" alt="Capture preview" />
    <div id="preview-buttons">
      <button id="btn-retake" class="btn btn-ghost">Retake</button>
      <button id="btn-approve" class="btn btn-primary">Send to Claude</button>
    </div>
  </div>
  <div id="permission-dialog" style="display: none;">
    <h2>Screen Recording Permission Required</h2>
    <p>snapview needs screen recording access to capture your screen. Open System Settings to grant permission.</p>
    <p class="sequoia-note">macOS may ask for screen recording permission monthly. This is expected.</p>
    <button id="btn-open-settings" class="btn btn-primary">Open System Settings</button>
  </div>
  <script type="module" src="./app.ts"></script>
</body>
</html>
```

**.gitignore:**
```
node_modules/
out/
dist/
*.tgz
.DS_Store
Thumbs.db
```

Run `bun install` after creating all files.
  </action>
  <verify>
    <automated>cd C:/Users/jdvis/Documents/Projects/snapview && bun install 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains `"name": "snapview"`
    - package.json contains `"electron": "35.7.5"`
    - package.json contains `"bin":` with `"snapview": "bin/snapview.cjs"`
    - electron.vite.config.ts contains `externalizeDepsPlugin`
    - tsconfig.main.json contains `"moduleResolution": "nodenext"`
    - tsconfig.renderer.json contains `"moduleResolution": "bundler"`
    - src/shared/types.ts exports `RegionRect`, `CaptureResult`, `AppPhase`, `IPC_CHANNELS`
    - src/renderer/index.html contains `id="overlay-canvas"` and `id="preview-panel"` and `id="hint-bar"`
    - .gitignore contains `node_modules/` and `out/`
    - `bun install` completes without errors
  </acceptance_criteria>
  <done>All project config files exist, types are defined, HTML shell has all required DOM elements, dependencies install cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Create CLI entry point that spawns Electron</name>
  <files>bin/snapview.cjs</files>
  <read_first>
    .planning/phases/01-capture-engine/01-RESEARCH.md
    package.json
  </read_first>
  <action>
Create `bin/snapview.cjs` — the globally-installed CLI entry point. This is a CommonJS Node.js script (no TypeScript, no Electron imports). It locates the Electron binary via `require('electron')` and spawns the built Electron app.

```javascript
#!/usr/bin/env node
'use strict';

const { spawn } = require('child_process');
const path = require('path');

// require('electron') returns the path to the Electron binary
const electronPath = require('electron');

// Point to the built main process entry point (electron-vite output)
const appPath = path.join(__dirname, '..', 'out', 'main', 'index.js');

// Forward CLI args to the Electron process (after stripping node + script path)
const child = spawn(electronPath, [appPath, ...process.argv.slice(2)], {
  // stdin + stderr inherited; stdout piped so we can capture the file path output
  stdio: ['inherit', 'pipe', 'inherit'],
});

// Forward Electron's stdout to our stdout (file path comes through here)
child.stdout.on('data', (data) => process.stdout.write(data));

// Mirror the Electron process exit code
child.on('exit', (code) => process.exit(code ?? 0));

// Handle spawn errors (e.g., electron binary not found)
child.on('error', (err) => {
  process.stderr.write(`[snapview] Failed to start Electron: ${err.message}\n`);
  process.exit(1);
});
```

Key points:
- `stdio: ['inherit', 'pipe', 'inherit']` — stdout piped for machine-parseable output (per locked decision)
- Uses `require('electron')` which returns the binary path (NOT `import`)
- CommonJS (`.cjs` extension) because it runs in bare Node.js, not Electron
- `appPath` points to `out/main/index.js` — the electron-vite build output directory
- Exit code pass-through: 0 = success, 1 = error, 2 = cancelled
  </action>
  <verify>
    <automated>node -e "const fs = require('fs'); const content = fs.readFileSync('C:/Users/jdvis/Documents/Projects/snapview/bin/snapview.cjs', 'utf8'); console.log(content.includes('spawn') && content.includes('electron') && content.includes('pipe') ? 'PASS' : 'FAIL')"</automated>
  </verify>
  <acceptance_criteria>
    - bin/snapview.cjs contains `#!/usr/bin/env node`
    - bin/snapview.cjs contains `require('electron')`
    - bin/snapview.cjs contains `spawn(electronPath`
    - bin/snapview.cjs contains `stdio: ['inherit', 'pipe', 'inherit']`
    - bin/snapview.cjs contains `child.stdout.on('data'`
    - bin/snapview.cjs contains `child.on('exit'`
    - bin/snapview.cjs contains `child.on('error'`
    - bin/snapview.cjs contains `process.exit(code ?? 0)`
  </acceptance_criteria>
  <done>CLI entry point exists, uses spawn (not execFileSync), pipes stdout for file path forwarding, passes through exit codes, handles spawn errors.</done>
</task>

</tasks>

<verification>
- `bun install` completes without errors
- All files exist at expected paths
- `node -c bin/snapview.cjs` parses without syntax errors
- `bun run typecheck` passes (once stub main/renderer files exist — may need empty stubs)
</verification>

<success_criteria>
- Project skeleton is fully scaffolded with all config files
- Shared types define the IPC contract used by all subsequent plans
- CLI entry point is ready to spawn Electron once the main process is built
- Dependencies are installed and lockfile is committed
</success_criteria>

<output>
After completion, create `.planning/phases/01-capture-engine/01-01-SUMMARY.md`
</output>
