---
phase: 01-capture-engine
plan: 04
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - src/main/capture.test.ts
  - src/main/cleanup.test.ts
  - src/main/index.test.ts
  - bin/snapview.test.cjs
autonomous: true
requirements:
  - CAPT-05
  - FILE-01
  - PLAT-01
  - PLAT-02
  - PLAT-03
  - PLAT-04
  - PLAT-05
  - PLAT-06
  - INST-02
  - INST-03

must_haves:
  truths:
    - "Unit tests verify PNG filename format matches snapview-{timestamp}-{hex}.png"
    - "Unit tests verify macOS permission gate is called before capture"
    - "Unit tests verify Linux GPU flags are applied when platform is linux"
    - "Unit tests verify hard-exit timeout is set to 30 seconds"
    - "Unit tests verify overlay bounds match the active display"
    - "Unit tests verify cleanup deletes files older than 24 hours"
    - "Unit tests verify CLI entry point spawns electron binary"
  artifacts:
    - path: "src/main/capture.test.ts"
      provides: "Tests for capture module — permission gate, filename format, PNG write, temp path"
      contains: "describe"
    - path: "src/main/cleanup.test.ts"
      provides: "Tests for cleanup module — file age filtering, error handling"
      contains: "describe"
    - path: "src/main/index.test.ts"
      provides: "Tests for main process — Linux GPU flags, hard exit, display bounds, Wayland"
      contains: "describe"
    - path: "bin/snapview.test.cjs"
      provides: "Tests for CLI entry point — spawn logic"
      contains: "describe"
  key_links:
    - from: "src/main/capture.test.ts"
      to: "src/main/capture.ts"
      via: "import functions under test"
      pattern: "import.*from.*capture"
    - from: "src/main/cleanup.test.ts"
      to: "src/main/cleanup.ts"
      via: "import sweepOldCaptures"
      pattern: "import.*sweepOldCaptures"
---

<objective>
Create the Wave 0 unit test suite for all testable main process logic. These tests mock Electron APIs and verify the core behaviors: macOS permission gating, filename format, temp dir usage, Linux GPU flags, hard-exit timeout, display bounds selection, cleanup file age filtering, and CLI spawn logic.

Purpose: Establish automated verification for all platform safety guards and file output logic. These tests run in under 10 seconds via `bun test` and serve as the regression safety net for the entire capture engine.
Output: Four test files covering capture, cleanup, main process, and CLI entry point.
</objective>

<execution_context>
@C:/Users/jdvis/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jdvis/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-capture-engine/01-RESEARCH.md
@.planning/phases/01-capture-engine/01-VALIDATION.md
@.planning/phases/01-capture-engine/01-02-SUMMARY.md

<interfaces>
<!-- Functions under test from capture.ts -->
From src/main/capture.ts:
```typescript
export async function checkMacOSPermission(): Promise<'granted' | 'denied' | 'unavailable'>;
export async function getScreenSources(): Promise<{ id: string; thumbnail: string }[]>;
export async function captureRegion(rect: RegionRect): Promise<CaptureResult>;
```

<!-- Functions under test from cleanup.ts -->
From src/main/cleanup.ts:
```typescript
export async function sweepOldCaptures(): Promise<void>;
```

<!-- From shared types -->
From src/shared/types.ts:
```typescript
export interface RegionRect { x: number; y: number; width: number; height: number; }
export interface CaptureResult { filePath: string; }
export const IPC_CHANNELS = {
  GET_SOURCES: 'capture:get-sources',
  CAPTURE_REGION: 'capture:region',
  CANCEL: 'capture:cancel',
} as const;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create unit tests for capture and cleanup modules</name>
  <files>src/main/capture.test.ts, src/main/cleanup.test.ts</files>
  <read_first>
    src/main/capture.ts
    src/main/cleanup.ts
    src/shared/types.ts
    .planning/phases/01-capture-engine/01-VALIDATION.md
  </read_first>
  <action>
Create test files using `bun:test` (built-in — no install needed). Import `describe`, `test`, `expect`, `mock`, `beforeEach`, `afterEach` from `bun:test`.

**IMPORTANT: These tests must mock Electron APIs** since `bun test` runs in Node/Bun, not in Electron. Use `bun:test` mock system to mock modules.

**src/main/capture.test.ts must contain these test groups:**

`describe('checkMacOSPermission')`:
- Test: returns 'granted' when `process.platform !== 'darwin'`
  - Mock `process.platform` to `'win32'`
  - Assert result is `'granted'`
- Test: returns 'granted' when getMediaAccessStatus returns 'granted'
  - Mock `process.platform` to `'darwin'`
  - Mock `systemPreferences.getMediaAccessStatus` to return `'granted'`
  - Assert result is `'granted'`
- Test: returns 'denied' when status is 'denied' and opens system settings
  - Mock `process.platform` to `'darwin'`
  - Mock `systemPreferences.getMediaAccessStatus` to return `'denied'`
  - Mock `shell.openExternal`
  - Assert result is `'denied'`
  - Assert `shell.openExternal` was called with `'x-apple.systempreferences:com.apple.preference.security?Privacy_ScreenCapture'`
- Test: calls getMediaAccessStatus on every invocation (not cached) — PLAT-05
  - Call `checkMacOSPermission()` twice
  - Assert `getMediaAccessStatus` was called exactly twice

`describe('captureRegion')`:
- Test: writes PNG file to `os.tmpdir()/snapview/` directory
  - Mock desktopCapturer.getSources to return a fake source with a mock nativeImage
  - Mock screen.getDisplayNearestPoint to return `{ scaleFactor: 1 }`
  - Mock screen.getCursorScreenPoint to return `{ x: 100, y: 100 }`
  - Mock fs.mkdir, fs.writeFile
  - Call `captureRegion({ x: 10, y: 10, width: 100, height: 100 })`
  - Assert fs.mkdir was called with path containing `os.tmpdir()` and `'snapview'`
  - Assert fs.writeFile was called with a path matching `/snapview-\d+-[a-f0-9]{16}\.png$/`
- Test: filename format matches `snapview-{timestamp}-{hex}.png` — FILE-01
  - From the writeFile mock call, extract the filename
  - Assert it matches regex: `/^snapview-\d{13,}-[a-f0-9]{16}\.png$/`
- Test: applies scaleFactor to rect coordinates for HiDPI
  - Mock `display.scaleFactor` to `2`
  - Assert the crop rect passed to `nativeImage.crop()` has coordinates multiplied by 2
- Test: temp dir path uses `os.tmpdir()` not hardcoded path — INST-03
  - Assert the directory path starts with `os.tmpdir()`

**src/main/cleanup.test.ts must contain these test groups:**

`describe('sweepOldCaptures')`:
- Test: deletes files older than 24 hours
  - Mock `fs.readdir` to return `['snapview-1000000000000-abc123.png']`
  - Mock `fs.stat` to return `{ mtimeMs: Date.now() - 25 * 60 * 60 * 1000 }` (25 hours ago)
  - Mock `fs.unlink`
  - Call `sweepOldCaptures()`
  - Assert `fs.unlink` was called
- Test: does NOT delete files younger than 24 hours
  - Mock `fs.stat` to return `{ mtimeMs: Date.now() - 1 * 60 * 60 * 1000 }` (1 hour ago)
  - Call `sweepOldCaptures()`
  - Assert `fs.unlink` was NOT called
- Test: ignores non-snapview files in the directory
  - Mock `fs.readdir` to return `['other-file.png', 'snapview-123-abc.png']`
  - Assert only files starting with `snapview-` and ending with `.png` are checked
- Test: handles missing directory gracefully (no throw)
  - Mock `fs.readdir` to throw ENOENT
  - Call `sweepOldCaptures()` — should not throw
- Test: handles unlink errors gracefully (no throw)
  - Mock `fs.unlink` to reject
  - Call `sweepOldCaptures()` — should not throw

**Mocking strategy notes:**
- Since Electron modules (`electron`, `electron/main`) are not available in bun test, you must mock the entire module at the top of the test file using `mock.module('electron', ...)` or equivalent bun:test mocking
- Create a mock factory that provides stubs for: `app`, `BrowserWindow`, `screen`, `desktopCapturer`, `systemPreferences`, `shell`, `ipcMain`, `nativeImage`
- For `fs/promises`: mock `mkdir`, `writeFile`, `readdir`, `stat`, `unlink`
- For `os`: mock `tmpdir()` to return a predictable path like `/tmp` or `C:\\Users\\test\\AppData\\Local\\Temp`

If direct module mocking is too complex for Electron internals, restructure tests to test the LOGIC by extracting testable pure functions from capture.ts. For example, test the filename generation, the scaleFactor multiplication, and the file age calculation as isolated functions.
  </action>
  <verify>
    <automated>cd C:/Users/jdvis/Documents/Projects/snapview && bun test src/main/capture.test.ts src/main/cleanup.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/main/capture.test.ts contains `describe('checkMacOSPermission'`
    - src/main/capture.test.ts contains test for non-darwin platform returning 'granted'
    - src/main/capture.test.ts contains test for 'denied' status opening system settings
    - src/main/capture.test.ts contains test verifying getMediaAccessStatus called on every invocation (PLAT-05)
    - src/main/capture.test.ts contains test for filename format `/snapview-\d+-[a-f0-9]+\.png/`
    - src/main/capture.test.ts contains test for scaleFactor multiplication
    - src/main/capture.test.ts contains test verifying `os.tmpdir()` usage (INST-03)
    - src/main/cleanup.test.ts contains test for deleting files older than 24 hours
    - src/main/cleanup.test.ts contains test for NOT deleting recent files
    - src/main/cleanup.test.ts contains test for handling missing directory
    - src/main/cleanup.test.ts contains test for handling unlink errors
    - `bun test` for both files exits with code 0
  </acceptance_criteria>
  <done>Capture and cleanup unit tests pass. Tests verify macOS permission gate, filename format, temp dir path, HiDPI scaling, and file age cleanup logic.</done>
</task>

<task type="auto">
  <name>Task 2: Create unit tests for main process and CLI entry point</name>
  <files>src/main/index.test.ts, bin/snapview.test.cjs</files>
  <read_first>
    src/main/index.ts
    bin/snapview.cjs
    .planning/phases/01-capture-engine/01-VALIDATION.md
  </read_first>
  <action>
**src/main/index.test.ts must contain these test groups:**

`describe('Linux GPU flags — PLAT-02')`:
- Test: `enable-transparent-visuals` switch is applied when `process.platform === 'linux'`
  - Since the flags are applied at module load time (before app.whenReady), test by reading the source file and verifying the pattern exists, OR by extracting the flag logic into a testable function
  - Verify that `app.commandLine.appendSwitch('enable-transparent-visuals')` is called on linux
  - Verify that `app.commandLine.appendSwitch('disable-gpu')` is called on linux
- Test: GPU flags are NOT applied on non-linux platforms

`describe('Hard exit timeout — PLAT-03')`:
- Test: timeout is set to 30000ms (30 seconds)
  - Verify the constant `HARD_EXIT_TIMEOUT_MS` equals `30_000` or `30000`
  - OR verify that `setTimeout` is called with the correct timeout value
- Test: timer is unref'd (does not keep process alive)

`describe('Overlay window bounds — PLAT-04')`:
- Test: BrowserWindow is created with bounds from the display nearest the cursor
  - Mock `screen.getCursorScreenPoint()` to return `{ x: 1920, y: 500 }` (second monitor)
  - Mock `screen.getDisplayNearestPoint()` to return `{ bounds: { x: 1920, y: 0, width: 1920, height: 1080 } }`
  - Verify BrowserWindow constructor receives `x: 1920, y: 0, width: 1920, height: 1080`
- Test: `fullscreen` is set to `false` (NOT true — true ignores x/y)

`describe('Wayland handler — PLAT-06')`:
- Test: `process.on('uncaughtException')` handler is registered
  - Verify the handler calls `app.exit(1)` or `app.quit()`

`describe('window-all-closed')`:
- Test: `app.on('window-all-closed')` handler calls `app.quit()`

**bin/snapview.test.cjs must contain:**

Use `const { describe, test, expect } = require('bun:test');` (or detect bun:test availability).

`describe('CLI entry point — INST-02')`:
- Test: snapview.cjs file exists and is syntactically valid
  - `require.resolve('../bin/snapview.cjs')` does not throw
- Test: file contains spawn call with electron path
  - Read the file content and verify it contains `require('electron')` and `spawn(`
- Test: stdio configuration pipes stdout
  - Verify file content contains `'pipe'` in the stdio array
- Test: exit code is forwarded from child process
  - Verify file content contains `process.exit(code`

**Mocking strategy for index.test.ts:**
Since main process code runs side effects at module load time (Linux flags, hard exit timer), consider one of these approaches:
1. Extract testable logic into separate functions that take dependencies as parameters
2. Use source code analysis (read the file and verify patterns exist) for load-time side effects
3. Mock the `electron` module before importing `index.ts`

For approach 2 (pragmatic for load-time side effects), read the source and verify:
- `appendSwitch('enable-transparent-visuals')` is guarded by `process.platform === 'linux'`
- `setTimeout` with `30_000` or `30000` is present
- `unref()` is called on the timer
- BrowserWindow config contains `fullscreen: false`

This is a valid testing strategy for code that executes side effects at import time.
  </action>
  <verify>
    <automated>cd C:/Users/jdvis/Documents/Projects/snapview && bun test src/main/index.test.ts bin/snapview.test.cjs 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/main/index.test.ts contains `describe` blocks for Linux GPU flags, hard exit timeout, overlay bounds, Wayland handler
    - src/main/index.test.ts verifies `enable-transparent-visuals` and `disable-gpu` on linux
    - src/main/index.test.ts verifies hard exit timeout value of 30000
    - src/main/index.test.ts verifies BrowserWindow bounds from display nearest cursor
    - src/main/index.test.ts verifies `fullscreen: false` in BrowserWindow config
    - src/main/index.test.ts verifies uncaughtException handler registration
    - bin/snapview.test.cjs verifies CLI file exists and contains `spawn` and `electron`
    - bin/snapview.test.cjs verifies stdio contains `pipe` for stdout
    - `bun test` for both files exits with code 0
  </acceptance_criteria>
  <done>All four test files pass. Unit tests cover: macOS permission gate, filename format, temp path, HiDPI scaling, cleanup age filter, Linux GPU flags, hard-exit timeout, display bounds, Wayland handler, and CLI spawn logic.</done>
</task>

</tasks>

<verification>
- `bun test` runs all four test files and exits 0
- Test output shows coverage of: CAPT-05, FILE-01, PLAT-01, PLAT-02, PLAT-03, PLAT-04, PLAT-05, PLAT-06, INST-02, INST-03
- No tests require Electron runtime (all mock appropriately)
</verification>

<success_criteria>
- All unit tests pass via `bun test`
- Tests verify every platform safety guard
- Tests verify file output format and location
- Tests run in under 10 seconds
- Wave 0 test gap from VALIDATION.md is closed
</success_criteria>

<output>
After completion, create `.planning/phases/01-capture-engine/01-04-SUMMARY.md`
</output>
