---
phase: 01-capture-engine
plan: 05
type: execute
wave: 4
depends_on:
  - 01-03
  - 01-04
files_modified: []
autonomous: false
requirements:
  - CAPT-01
  - CAPT-02
  - CAPT-03
  - CAPT-04

must_haves:
  truths:
    - "electron-vite build produces output in out/ directory without errors"
    - "Running snapview from CLI launches the overlay on the active monitor"
    - "Dragging a selection shows a clear cutout with border"
    - "Releasing the mouse shows preview panel with Send to Claude and Retake"
    - "Clicking Send to Claude writes PNG and outputs file path to stdout"
    - "Pressing ESC closes cleanly with exit code 2"
    - "All unit tests pass"
  artifacts: []
  key_links:
    - from: "bin/snapview.cjs"
      to: "out/main/index.js"
      via: "spawn — built output must exist"
      pattern: "out/main/index.js"
---

<objective>
Build the project, run all tests, and verify the complete capture flow end-to-end on the host machine. This is the integration checkpoint that confirms everything works together before the phase is considered complete.

Purpose: Catch integration issues — build errors, IPC mismatches, path resolution problems — that unit tests cannot detect.
Output: Verified working capture tool.
</objective>

<execution_context>
@C:/Users/jdvis/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jdvis/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-capture-engine/01-CONTEXT.md
@.planning/phases/01-capture-engine/01-03-SUMMARY.md
@.planning/phases/01-capture-engine/01-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build project and run full test suite</name>
  <files>out/main/index.js, out/preload/preload.js, out/renderer/index.html</files>
  <read_first>
    package.json
    electron.vite.config.ts
  </read_first>
  <action>
1. Run `bun run build` (electron-vite build) — must complete without errors
2. Verify output directory structure exists:
   - `out/main/index.js` must exist
   - `out/preload/preload.js` must exist
   - `out/renderer/index.html` must exist
3. Run `bun test` — all unit tests must pass
4. Run `bun run typecheck` — TypeScript compilation must succeed with no errors
5. If any step fails, fix the issue before proceeding to the checkpoint

If the build fails due to import resolution or configuration issues, diagnose and fix:
- Check electron-vite config for correct externalizeDepsPlugin usage
- Check tsconfig paths and moduleResolution settings
- Verify all imports use correct relative paths
- Ensure shared/types.ts is included in both tsconfig include arrays
  </action>
  <verify>
    <automated>cd C:/Users/jdvis/Documents/Projects/snapview && bun run build 2>&1 | tail -10 && echo "---BUILD_DONE---" && bun test 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `bun run build` exits with code 0
    - `out/main/index.js` exists
    - `out/preload/preload.js` exists
    - `out/renderer/index.html` exists
    - `bun test` exits with code 0 with all tests passing
  </acceptance_criteria>
  <done>Project builds successfully and all unit tests pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify complete capture flow on host machine</name>
  <files></files>
  <action>
Present the verification checklist to the user. The build is complete from Task 1 — this task is a manual verification checkpoint where the user runs the app and confirms the capture flow works end-to-end.

Launch command: `node bin/snapview.cjs` from the project root directory.
  </action>
  <verify>User confirms all verification steps pass by typing "approved".</verify>
  <done>User has verified: overlay appears on active monitor with dim and crosshair, drag-to-select shows cutout, preview panel works with Send to Claude and Retake, PNG file is written and path output to stdout, ESC cancels cleanly with exit code 2.</done>
  <what-built>
Complete Snapview capture engine — Electron overlay with drag-to-select region capture, preview panel with Send to Claude / Retake, PNG output to temp directory, and ESC cancellation. All platform safety guards are in place.
  </what-built>
  <how-to-verify>
1. Open a terminal in the project directory
2. Run `node bin/snapview.cjs` (or `bun run dev` for dev mode)
3. **Verify overlay appears:**
   - A dimmed overlay should cover your active monitor (not all monitors)
   - Cursor should be a crosshair
   - "Drag to select  ·  ESC to cancel" hint should be visible at the bottom
4. **Verify selection:**
   - Click and drag to select a screen region
   - The selected area should appear at full brightness (clear cutout through the dim)
   - A subtle white border should appear around the selection
5. **Verify preview:**
   - After releasing the mouse, a centered preview panel should appear
   - Panel should show "Capture Preview" heading, the cropped image, "Send to Claude" and "Retake" buttons
   - Click "Retake" — should return to the selection overlay
6. **Verify capture output:**
   - Select a new region and click "Send to Claude"
   - The app should close
   - The terminal should show a file path like `C:\Users\...\AppData\Local\Temp\snapview\snapview-1234567890123-abcdef0123456789.png`
   - Verify the file exists at that path and opens as a valid PNG
7. **Verify ESC cancellation:**
   - Run `node bin/snapview.cjs` again
   - Press ESC immediately
   - The overlay should close cleanly
   - Check exit code: `echo $?` should show `2`
8. **Verify click-without-drag cancels:**
   - Run again, single click without dragging
   - Should cancel and close (exit code 2)
  </how-to-verify>
  <resume-signal>Type "approved" if the capture flow works correctly, or describe any issues you see (e.g., "overlay is opaque", "preview doesn't show image", "ESC doesn't work").</resume-signal>
</task>

</tasks>

<verification>
- Build passes, tests pass, typecheck passes
- Manual verification of the full capture flow on host machine
- File output verified at the expected temp directory path
</verification>

<success_criteria>
- electron-vite build produces working output
- All unit tests green
- User confirms: overlay appears, selection works, preview works, capture saves PNG, ESC cancels cleanly
- Phase 1 capture engine is functionally complete
</success_criteria>

<output>
After completion, create `.planning/phases/01-capture-engine/01-05-SUMMARY.md`
</output>
